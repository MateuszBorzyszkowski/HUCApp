HUCApp commands

$ java -jar [jar name]
$ [option] ([parameter] [name],...)

description:
    add
        vendor
            -name, -n
            -service, -s

    remove
        vendor
            -name, -n

    show
        vendor

    exit

Example:
$ add vendor -name nameOfVendor -service gas
$ remove vendor -name nameOfVendor


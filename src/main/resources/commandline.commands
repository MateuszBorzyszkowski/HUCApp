HUCApp commands

$ java -jar [jar name]
$ [option] ([parameter] [name],...)

description:
    add
        vendor
            -name, -n
            -service, -s
        place
            -name, -n
            -address, -a
            -postcode, -p
            -city, -c

    remove
        vendor
            -name, -n
        place
            -name, -n

    show
        vendor
        place

    exit

Example:
$ add vendor -name nameOfVendor -service gas
$ remove vendor -name nameOfVendor

